# Database Postgres (obbligatorio: locale e Vercel)
# Locale: Neon, Docker o altro Postgres
# Vercel: Storage → Create Database → Postgres (Vercel imposta DATABASE_URL)
DATABASE_URL="postgresql://user:password@host:5432/dbname?sslmode=require"

# Cifratura credenziali clienti (client_credentials). OBBLIGATORIA in produzione.
# Genera con: npx tsx scripts/generate-credentials-key.ts
# Vercel: Project → Settings → Environment Variables → CREDENTIALS_ENCRYPTION_KEY (Production)
CREDENTIALS_ENCRYPTION_KEY=

# Clerk - Autenticazione (opzionale; se assenti l'app usa /dev-users solo in sviluppo)
# Su Vercel: imposta anche NEXT_PUBLIC_APP_URL con l'URL di produzione (es. https://xxx.vercel.app)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_...
CLERK_SECRET_KEY=sk_live_...
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_APP_URL=http://localhost:3000
